version: 1
applications:
  - appRoot: ui
    env:
      variables:
        TEST: foo
    frontend:
      buildPath: ui
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - env | grep -e NEXT_PUBLIC_API_URL >> .env
            - env | grep -e WORKOS_CLIENT_ID >> .env
            - env | grep -e WORKOS_API_KEY >> .env
            - env | grep -e WORKOS_REDIRECT_URI >> .env
            - env | grep -e JWT_SECRET_KEY >> .env
            - env | grep -e WORKOS_COOKIE_PASSWORD >> .env
            - env | grep -e ROOTKEY_PASSWORD >> .env
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
            - '**/*'
        discard-paths: yes
      cache:
        paths:
            - node_modules/**/*
